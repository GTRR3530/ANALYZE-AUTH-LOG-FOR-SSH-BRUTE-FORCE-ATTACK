SSH Brute Force Authentication Logs
Description

This repository contains Linux authentication (auth.log) entries that demonstrate an SSH brute force attack. The logs show repeated failed SSH login attempts from one or more sources attempting to gain unauthorized access.

Purpose

These logs are intended for:

Security analysis and log review practice

Intrusion detection and SIEM testing

Learning how SSH brute force attacks appear in system logs

Blue team / SOC training exercises

Contents

auth.log – Authentication log containing failed SSH login attempts

Key Indicators

Multiple failed SSH login attempts

Repeated authentication failures within short time intervals

Attempts using different usernames from the same or multiple IP addresses
